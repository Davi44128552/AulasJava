package Java.aula.aula7;

public class aula7 {

    public static void main(String[] args){

        // String = a tipo de dado que armazena um ou mais caracteres

        // String nome = "Davi";

      /*  boolean resultado = nome.equals("Davi"); // Verifica se a string equivale ao que é escrito
        boolean resultado2 = nome.equalsIgnoreCase("Davi"); // Igual ao de cima, ignorando dirença entre maiúsculas e minúsculas
        int letras = nome.length(); // Conta o número de caracteres em uma string
        char letra = nome.charAt(0); // Retorna o caractere de uma string em determinada posição
        int index = nome.indexOf("e"); // Retorna a posição de determinada letra
        boolean resultado3 = nome.isEmpty(); // Verifica se a string está vazia ou não
        String nome_maiusculo = nome.toUpperCase(); // Converter a string para letras maiúsculas
        String nome_minusculo = nome.toLowerCase(); // Converter a string para letras minúsculas
        String nome_corrigido = nome.trim(); // Remove todos os espaços desncessários
        String novo_nome = nome.replace("a", "e"); // Substituiu uma letra de uma string para outra
 */
    }
    
}
